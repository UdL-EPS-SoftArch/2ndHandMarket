import { } from '@angular/core/testing';
import {BasketProduct} from './basketProduct';

describe('BasketProduct', () => {
  it('should create an instance', () => {
    expect(new BasketProduct()).toBeTruthy();
  });
});
